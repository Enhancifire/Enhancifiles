import { Node, Document, LineCounter } from 'yaml';
import { ASTNode } from '../jsonASTTypes';
declare type NodeRange = [number, number, number];
export declare function convertAST(parent: ASTNode, node: Node, doc: Document, lineCounter: LineCounter): ASTNode;
export declare function toOffsetLength(range: NodeRange): [number, number];
export {};
